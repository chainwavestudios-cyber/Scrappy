# Copy this file to cities/<your_city>.py and fill in the values.
# Rename to e.g. riverside.py — the .example suffix prevents it from loading.

CONFIGS = {
    'riverside': {
        'name':        'Riverside',
        'base_url':    'https://aca-prod.accela.com/RIVERSIDE',
        # Optional: if the portal must load from a different entry than Cap/CapHome (see San Diego)
        # 'portal_url': 'https://example.gov/CitizenAccess/Default.aspx',
        'module':      'Building',
        'permit_type': None,  # Or specific label, e.g. 'Residential Solar Energy'
        'source':      'riverside_accela',
        # Optional overrides (defaults in scraper):
        # 'col_date': 1, 'col_permit_num': 3, 'col_description': 4, ...
        # 'lead_category': 'residential',
        # 'owner_from_contacts': False,
    },
}
